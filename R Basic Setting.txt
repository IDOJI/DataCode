# @ [0] Basic Setting ============================================================================================================================================================================## @ [0-1] Loading packages ======================================================================================# rm(list=ls())path_OS = "C:/Users/lleii/" # Windowspath_OS = "/Users/Ido/" # Macinstall_packages = function(packages, load=TRUE) {  # load : load the packages after installation?  for(pkg in packages) {    if (!require(pkg, character.only = TRUE)) {      install.packages(pkg)    }        if(load){      library(pkg, character.only = TRUE)    }  }}install_packages(c("tidyverse", "dplyr", "clipr", "fda", "tidyr", "stringr", "gridExtra", "zoo"))## @ [0-2] Loading functions ============================================================================================================================================================================path_Dropbox = paste0(path_OS, "Dropbox")path_GitHub = list.files(path_Dropbox, pattern = "Github", full.names = T)path_Rpkgs = list.files(path_GitHub, pattern = "Rpkgs", full.names = T)Rpkgs = c("ADNIprep", "StatsR", "refineR", "dimR")Load = sapply(Rpkgs, function(y){  list.files(paste0(path_Rpkgs, "/", y, "/R"), full.names = T) %>% walk(source) })## @ [0-3] Setting path ============================================================================================================================================================================# Datapath_Data = paste0(path_Dropbox, "/Papers Data")### @ Depth 1 ============================================================================================================================================================================# ADNIpath_ADNI = list.files(path_Data, full.names = T, pattern = "ADNI")### @ Depth 2 ============================================================================================================================================================================# RS-fMRIpath_RSfMRI = list.files(path_ADNI, full.names = T, pattern  = "fMRI")# SNPpath_SNP = list.files(path_ADNI, full.names = T, pattern  = "SNP")# Subjects Listpath_SubjectsList_Old = paste0(path_ADNI, "/Subjects Lists/Old/Subjects_Lists_Exported/Final/[Final_Selected]_Subjects_list.csv")### @ Depth 3 ============================================================================================================================================================================# BOLD Signalspath_BOLDSignals = list.files(path_RSfMRI, full.names = T, pattern = "BOLD")# Functional Data from RS-fMRIpath_FunctionalData = list.files(path_RSfMRI, full.name = T, pattern  ="Functional Data")